node_modules/
.next/
.env.local
.env*.local
*.log
.DS_Store
/build
/out
next-env.d.ts
*.tsbuildinfo

# Ảnh hiện trường upload từ app — không đưa vào git
data/uploads/

# Binary + log của cloudflared tunnel — chạy local, không đưa vào git
cloudflared.exe
cloudflared-*.txt

# Skill/agent của công cụ dev — không đưa vào repo dự án
.agents/

# SQLite cũ (đã chuyển sang Postgres) — giữ file local làm backup
xboss.db
xboss.db-shm
xboss.db-wal

# Bộ khung tạm — staging để tự merge, không commit (xem docs/framework/AP-DUNG-vao-du-an-co-san.md)
_framework-dropins/
*.framework-new

# Tarball backup nặng — không commit
xboss.tar.gz
.env.example.framework-new

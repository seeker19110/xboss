# MEP-Agents stack: redis, postgres(pgvector), api, worker, streamlit, web
# Phase C adds postgres service. Usage: cp .env.example .env && docker compose up --build

services:
  redis:
    image: redis:7-alpine
    restart: unless-stopped
    # Redis vừa là broker vừa là result backend của Celery: ai ghi được vào đây là điều
    # khiển được Worker, mà Worker có quyền đọc/ghi toàn bộ thư mục bản vẽ. Trước đây
    # service này chạy KHÔNG mật khẩu — bất kỳ container nào trong mạng Compose (hoặc bất
    # kỳ ai vào được mạng đó) đều đẩy được lệnh vào hàng đợi.
    #
    # `:?` là cố ý: thiếu REDIS_PASSWORD thì `docker compose up` DỪNG kèm thông báo rõ,
    # thay vì lặng lẽ chạy mở toang. Đây là chỗ duy nhất trong dự án không áp dụng
    # "graceful fallback" — một hàng đợi mở không phải là suy giảm nhẹ nhàng, mà là mất
    # quyền kiểm soát Worker.
    command: ["redis-server", "--requirepass", "${REDIS_PASSWORD:?Dat REDIS_PASSWORD trong .env truoc khi chay}"]
    # KHÔNG mở `ports:` ra host: các service khác gọi qua mạng nội bộ của Compose bằng tên
    # "redis". Mở ra host là đưa hàng đợi ra Internet nếu máy chủ có IP công cộng.
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD}", "--no-auth-warning", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5

  postgres:
    image: pgvector/pgvector:pg16
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-mep}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-mep_secret}
      POSTGRES_DB: ${POSTGRES_DB:-mep_agents}
    ports:
      # Chỉ loopback: mở "5432:5432" là nghe trên MỌI địa chỉ của máy chủ, kèm mật khẩu
      # mặc định trong file này thì cơ sở dữ liệu coi như công khai.
      - "127.0.0.1:5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-mep} -d ${POSTGRES_DB:-mep_agents}"]
      interval: 10s
      timeout: 5s
      retries: 5

  api:
    build: .
    restart: unless-stopped
    command: ["uv", "run", "uvicorn", "src.api:app", "--host", "0.0.0.0", "--port", "8083"]
    ports:
      - "8083:8083"
    env_file:
      - .env
    environment:
      CELERY_BROKER_URL: redis://:${REDIS_PASSWORD}@redis:6379/0
      CELERY_RESULT_BACKEND: redis://:${REDIS_PASSWORD}@redis:6379/0
      REDIS_HOST: redis
      REDIS_PASSWORD: ${REDIS_PASSWORD}
      CORS_ALLOWED_ORIGINS: ${CORS_ALLOWED_ORIGINS:-http://localhost:5173}
      DATABASE_URL: ${DATABASE_URL:-postgresql://mep:mep_secret@postgres:5432/mep_agents}
      USE_PGVECTOR: ${USE_PGVECTOR:-false}
    depends_on:
      redis:
        condition: service_healthy
      postgres:
        condition: service_healthy
    volumes:
      - uploads_data:/app/uploads
      - boq_data:/app/data/boq
      - workspaces_data:/app/data/workspaces
    healthcheck:
      test: ["CMD", "uv", "run", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8083/')"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 20s

  worker:
    build: .
    restart: unless-stopped
    command: ["uv", "run", "celery", "-A", "src.celery_app", "worker", "-l", "info", "--concurrency=2"]
    env_file:
      - .env
    environment:
      CELERY_BROKER_URL: redis://:${REDIS_PASSWORD}@redis:6379/0
      CELERY_RESULT_BACKEND: redis://:${REDIS_PASSWORD}@redis:6379/0
      REDIS_HOST: redis
      REDIS_PASSWORD: ${REDIS_PASSWORD}
      DATABASE_URL: ${DATABASE_URL:-postgresql://mep:mep_secret@postgres:5432/mep_agents}
    depends_on:
      redis:
        condition: service_healthy
      postgres:
        condition: service_healthy
    volumes:
      - uploads_data:/app/uploads
      - boq_data:/app/data/boq
      - workspaces_data:/app/data/workspaces

  streamlit:
    build: .
    restart: unless-stopped
    ports:
      - "8501:8501"
    env_file:
      - .env
    environment:
      CELERY_BROKER_URL: redis://:${REDIS_PASSWORD}@redis:6379/0
      CELERY_RESULT_BACKEND: redis://:${REDIS_PASSWORD}@redis:6379/0
      REDIS_HOST: redis
      REDIS_PASSWORD: ${REDIS_PASSWORD}
      DATABASE_URL: ${DATABASE_URL:-postgresql://mep:mep_secret@postgres:5432/mep_agents}
    depends_on:
      redis:
        condition: service_healthy

  web:
    build:
      context: ./web
      args:
        VITE_API_BASE: ${VITE_API_BASE:-http://localhost:8083}
    restart: unless-stopped
    ports:
      - "5173:80"
    depends_on:
      - api

volumes:
  redis_data:
  postgres_data:
  uploads_data:
  boq_data:
  workspaces_data:

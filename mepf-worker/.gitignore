.venv/
__pycache__/
*.pyc
.env
*.faiss
*.pkl

# Bỏ qua tất cả các file bản vẽ và báo cáo dự án
*.dxf
*.dwg
*.xlsx
*.xls
*.pdf
*.docx

# Giữ lại duy nhất Thư viện Block mẫu chuẩn của hệ thống
!data/blocks/mepf_library.dxf

# Thư mục làm việc riêng theo từng phiên Streamlit (per-session workspace, xem src/workspace.py)
outputs/

# Checkpoint hội thoại (SQLite) — dữ liệu chạy, không commit
data/checkpoints.sqlite*
.redis/
uploads/

# CSDL người dùng sinh ra lúc chạy — không bao giờ commit (chứa hash mật khẩu thật)
data/users.sqlite
data/workspaces/

# CSDL Project Kernel sinh ra lúc chạy (src/project_kernel.py) — không commit
data/project_kernel.sqlite

# Goal — Đóng các khoảng trống audit XBoss 2026-09-25

## 1. Outcome và trạng thái

Mục tiêu: cách ly đúng user/org/project từ API tới offline; tiền và báo cáo đối soát chính
xác; chuỗi BOQ–nghiệm thu–thanh toán có bất biến/concurrency/audit; backup phục hồi được với
bằng chứng. Không mở rộng module sản phẩm hoặc thay stack ngoài việc cần cho mục tiêu này.

State: **SPEC / In review**. Chủ dự án yêu cầu hoàn thiện đặc tả để thi hành sau.
Spec entry: [README](../nang-cap/AUDIT-2026-09-25/README.md).
Kế hoạch: [PLAN](../nang-cap/AUDIT-2026-09-25/PLAN.md).
Quyết định: [APPROVAL](../nang-cap/AUDIT-2026-09-25/APPROVAL.md).
Bằng chứng: [TEST-MATRIX](../nang-cap/AUDIT-2026-09-25/TEST-MATRIX.md).

## 2. Baseline tại lần đặc tả

Main đọc 2026-09-25: a2b9d7b9d28a839a5ee9cf2b23fc6b386ca292b3.
Đợt 1: PR #529, head 2641b6f733ae157a188168aba162fb8c081c3e2f; code đã push, CI run
36095713458 thành công, PR vẫn mở/chưa merge tại lần đọc. Chưa đồng nghĩa production đã đổi.
Trước thi hành reload repo thật, kể cả khi ngày trùng; không lấy checkpoint này thay trạng thái mới.

PR đặc tả là nhánh docs/audit-20260925-execution-spec từ main, không đổi PR529 và không mang
code PR529 vào main. Chỉ thêm tài liệu; không thực thi DDL, migration, reset/seed, provider hoặc
deploy. Không sửa/xóa lịch sử PROGRESS.md và không thay root PLAN.md đang có.
Sau khi các PR được merge thực tế, coordinator cập nhật PROGRESS.md phù hợp với bản trên main;
trước đó checkpoint này giữ trạng thái riêng để tránh conflict và claim hoàn thành sai.

## 3. Milestones và khoảng cách còn lại

A1: resolver, membership, org-session, caller coverage, transaction/RLS — chưa thực hiện trong
PR tài liệu. A2: network-only/allowlist, context, queue v2/receipt/conflict/legacy — chưa thực hiện.
A3: exact money/DTO/export và quy tắc chứng từ — chưa thực hiện. A4: canonical costs,
coverage/totals và portfolio task-weighted — chưa thực hiện. A5: chain fixtures, qty cap,
concurrency/downstream reversal — chưa thực hiện. A6: manifest/verifier/restore/UAT/release —
chưa thực hiện. Chi tiết và các đề xuất cần duyệt nằm trong từng chương.

Không diễn giải “đặc tả đủ chương” thành “code đủ tính năng” hoặc “hệ thống đã an toàn”.
Các phát hiện từ đọc code phải có test tái hiện trước khi đánh dấu confirmed/fixed.

## 4. Goal DoD và guardrails

- [ ] Bất biến đợt 1 và mọi slice bắt buộc đã có bằng chứng trên main.
- [ ] 46 AC trong TEST-MATRIX đã PASS với bằng chứng đúng lớp; không còn critical NOT_RUN.
- [ ] Không còn P0/P1 phạm vi goal, mapping/DDL/backfill/queue legacy dang dở.
- [ ] Không rò chéo scope, mất draft đã ACK lưu, nhân đôi business effect hoặc sai tiền exact.
- [ ] Scope/RLS bằng app role thật, regression, concurrency, desktop/mobile/axe/offline đạt.
- [ ] SQL/API/export và nguồn BOQ/contract/payment đối soát; UAT đủ vai trò.
- [ ] Restore recovery set có dữ liệu/file/key reference và số đo RPO/RTO được owner duyệt.
- [ ] ADR/ERD/runbook/telemetry/ownership và residual risks cập nhật.
- [ ] Phê duyệt riêng của chủ dự án cho phát hành/production verification nếu nằm trong lần thi hành.

Mốc CODE_COMPLETE chỉ kết luận code/test; RELEASE_VERIFIED cần diễn tập/UAT/production evidence.
RPO24h/RTO4h, offline lease15phút, p95 không tệ hơn20% và cửa sổ quan sát24h là đề xuất trong
spec, chưa đo/chưa duyệt như SLA. Không đánh dấu đạt từ một pipeline docs-only.

## 5. Checkpoint bàn giao

```text
Iteration: đặc tả 2026-09-25 | State: SPEC / In review
Main SHA: a2b9d7b9d28a839a5ee9cf2b23fc6b386ca292b3
Goal gap: A1–A6 còn implementation và verification
Dependency: #529 đang mở tại lần kiểm tra
Decision: hoàn thiện tài liệu; chưa thi hành code/migration/production
Blocker: approval chương/slice và mapping main mới trong S00
Next slice: S00 reconcile/inventory; chọn slice READY sau khi đủ approval/dependencies
Permission needed: implementation; merge/production/dữ liệu thật cấp riêng
```

Mỗi vòng tiếp theo thêm checkpoint có ngày/SHA thật, link PR/evidence và thay đổi gap.
Không tự tick milestone từ mô tả PR nếu thiếu diff/tests, không dùng CI của SHA cũ thay HEAD.
Cùng failure tối đa ba repair attempts theo AI_DELIVERY_LOOP; cần đổi contract thì BLOCKED
và quay lại duyệt spec, không nới guardrail để tiếp tục.
